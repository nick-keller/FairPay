{% extends '@FerusMail/Template/layout.html.twig' %}

{% block content %}

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th></th>
            </tr>
            {% for template in templates %}
                <tr>
                    <td>{{ template.id }}</td>
                    <td><a href="{{ path('template_admin_edit', {id:template.id}) }}">{{ template.name }}</a></td>
                    <td>
                        <div class="btn-group btn-group-xs">
                            <a class="btn btn-default" href="{{ path('template_admin_edit', {id:template.id}) }}">
                                {{ icon('pencil') }} Editer
                            </a>
                            <a class="btn btn-default" href="{{ path('template_admin_remove', {id:template.id}) }}">
                                {{ icon('remove') }}
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}