{% extends '@FerusMail/Authority/layout.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ authority }}</li>
    <li class="active">Supprimer</li>
{% endblock %}

{% block content %}
    <div class="alert alert-warning" role="alert">
        <h2>Attention !</h2>
        <p>Vous êtes sur le point de supprimer définitivement <b>{{ authority }}</b>. Voulez vous continuer ?</p>
        <form method="post" style="margin-top: 10px">
            <button type="submit" class="btn btn-danger">
                {{ icon('remove') }} Supprimer {{ authority }}
            </button>
            <a class="btn btn-default" href="{{ path('authority_admin_index') }}">
                Annuler
            </a>
        </form>
    </div>
{% endblock %}