{% extends '@FerusEvent/Admin/layout.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="active">Tous les évenements</li>
{% endblock %}

{% block content %}
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Date</th>
                <th></th>
            </tr>
            {% for event in events %}
                <tr>
                    <td>{{ event.id }}</td>
                    <td>
                        <a href="{{ path('event_admin_edit', {id:event.id}) }}">
                            {{ event }}
                        </a>
                    </td>
                    <td>
                        {{ event.date|date('d/m Y') }}
                    </td>
                    <td>
                        <div class="btn-group btn-group-xs">
                            <a href="{{ path('event_admin_edit', {id:event.id}) }}" class="btn btn-default">
                                {{ icon('pencil') }} <span class="visible-sm-inline visible-md-inline visible-lg-inline">Edit</span>
                            </a>
                            <a type="button" class="btn btn-default" href="{{ path('event_admin_remove', {id:event.id}) }}">
                                {{ icon('remove') }}
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
    {{ knp_pagination_render(events) }}
{% endblock %}